import 'package:flutter/cupertino.dart';

class MyBoxesPage extends StatelessWidget {
  const MyBoxesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: const Text('MyBoxes'),);
  }
}
