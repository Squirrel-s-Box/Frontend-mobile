import 'package:flutter/material.dart';

class SplashInitPage extends StatelessWidget {
  const SplashInitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('SplashInitPage'),
      ),
    );
  }
}
